var config = {};

config.database = {};  // database config

//database url
config.database.url = "localhost";
config.database.user = "crossover";
config.database.password = "crossover";
config.database.schema = "crossover";

//port configuration
config.port = 80;

module.exports = config;